name: Backend Tests

on:
  push:
    paths:
      - "Backend/TeapotApi/**"
      - ".github/workflows/backend-tests.yml"
  pull_request:
    paths:
      - "Backend/TeapotApi/**"
      - ".github/workflows/backend-tests.yml"

jobs:
  test:
    name: Dotnet Tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: "10.0.x"

      - name: Restore
        run: dotnet restore Backend/TeapotApi/TeapotApi.slnx

      - name: Test
        run: dotnet test Backend/TeapotApi/TeapotApi.slnx --no-restore --verbosity normal
